# Cryptography Lab - Instructions

## Compilation:
To compile the program, use the following command:
```
gcc specks_cbc.c specks.c -o specks_cbc.run
```

## Execution
To run the program, use the following command:
```
./specks_cbc.run
```